export default class GetFirstItem {
        
        /**
        * get first item
        * @param array
        * @returns string
        * */
        public exec(array:string[]): string {
            return array[0];
        }
}